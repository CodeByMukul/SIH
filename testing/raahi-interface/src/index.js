import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Ensure Tailwind CSS is applied
import RaahiInterface from './App';

ReactDOM.render(
  <React.StrictMode>
    <RaahiInterface />
  </React.StrictMode>,
  document.getElementById('root')
);
